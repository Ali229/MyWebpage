# Project Workflow Rules

- Do not run builds, `ng serve`, dev servers, browser checks, or other runtime verification commands unless the user explicitly asks for them. The user will run those manually.
